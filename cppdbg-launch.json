{
  "version": "0.2.0",
  "inputs": [
    {
      "id": "promptString",
      "description": "prompt a string",
      "type": "promptString",
    }
  ],
  "configurations": [
    {
      "name": "cppdbg - Launch",
      "type": "cppdbg",
      "request": "launch",
      "program": "${workspaceFolder}/${input:promptString}",
      "args": [],
      "stopAtEntry": false,
      "cwd": "${workspaceFolder}",
      "environment": [],
      "externalConsole": false,
      "MIMode": "gdb" 
    }
  ]
}
